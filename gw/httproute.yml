apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: backend
spec:
  parentRefs:
    - name: eg
  hostnames:
    - "www.myapp.io"
  rules:
    - backendRefs:
        - group: ""
          kind: Service
          name: go-web-app
          port: 80
          weight: 1
      matches:
        - path:
            type: PathPrefix
            value: /
